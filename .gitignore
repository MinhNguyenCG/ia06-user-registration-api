# Environment variables
.env
.env.local
.env.*.local

# MongoDB data (if running locally without Docker)
data/

# Docker volumes are managed by Docker, but we can ignore local data directories
mongodb_data/

